# Sanity v3 — front-end (Vite) reads VITE_-prefixed vars
VITE_SANITY_PROJECT_ID=your_project_id_here
VITE_SANITY_DATASET=production
VITE_SANITY_API_VERSION=2024-01-01

# Sanity Studio (read from studio/sanity.config.js)
SANITY_STUDIO_PROJECT_ID=your_project_id_here
SANITY_STUDIO_DATASET=production

# Note: no deploy hook needed — the site reads Sanity content client-side at
# runtime via the CDN, so editorial publishes go live without a redeploy.

# Iris voice agent (/iris) — server-side ONLY, used by api/iris-token.js to mint
# ephemeral Gemini Live tokens. No VITE_ prefix on purpose: it must never reach
# the browser bundle. Set this in the Vercel project env (Production + Preview)
# and in a local .env for `vercel dev`. Get a key: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_google_ai_studio_key_here
